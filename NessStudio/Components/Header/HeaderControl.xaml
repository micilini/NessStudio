<UserControl x:Class="NessStudio.Components.Header.HeaderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NessStudio.Components.Header"
                     xmlns:shell="http://schemas.microsoft.com/netfx/2009/xaml/presentation"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="IconButtonClickScale" TargetType="Button">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter
            Content="{TemplateBinding Content}"
            ContentTemplate="{TemplateBinding ContentTemplate}"
            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
            Margin="{TemplateBinding Padding}"
            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
            RecognizesAccessKey="True"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <Style.Triggers>
                <Trigger Property="IsPressed" Value="True">
                    <Trigger.EnterActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation
                Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                To="0.9" Duration="0:0:0.08"/>
                                <DoubleAnimation
                Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                To="0.9" Duration="0:0:0.08"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.EnterActions>
                    <Trigger.ExitActions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation
                Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleX)"
                To="1" Duration="0:0:0.12"/>
                                <DoubleAnimation
                Storyboard.TargetProperty="RenderTransform.(ScaleTransform.ScaleY)"
                To="1" Duration="0:0:0.12"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </Trigger.ExitActions>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <WrapPanel VerticalAlignment="Center" Margin="15 0 0 0" Grid.Column="0">
            <Image RenderOptions.BitmapScalingMode="HighQuality" Source="/Assets/Images/logo-nessstudio.png" Width="35" Margin="0 0 0 0"></Image>
            <TextBlock Text="NESS STUDIO" FontSize="22" FontWeight="Bold" FontFamily="Arial" Foreground="#e74c3c" Margin="6 6 0 0"></TextBlock>
        </WrapPanel>

        <WrapPanel VerticalAlignment="Center" Grid.Column="1" HorizontalAlignment="Right">

            <Button Style="{StaticResource IconButtonClickScale}" shell:WindowChrome.IsHitTestVisibleInChrome="True" Command="{Binding MinimizeApplicationCommand}" Padding="0" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="{DynamicResource PrimaryHueLightForegroundBrush}" Width="20" Height="20" Margin="0 0 20 0">
                <Image RenderOptions.BitmapScalingMode="HighQuality" Source="/Assets/Images/minimize-icon-white.png" Width="20" ></Image>
            </Button>

            <Button Style="{StaticResource IconButtonClickScale}" shell:WindowChrome.IsHitTestVisibleInChrome="True" Command="{Binding MaximizeApplicationCommand}" Padding="0" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="{DynamicResource PrimaryHueLightForegroundBrush}" Width="20" Height="20" Margin="0 0 20 0">
                <Image RenderOptions.BitmapScalingMode="HighQuality" Source="{Binding MaximizedIcon}" Width="20" ></Image>
            </Button>

            <Button Style="{StaticResource IconButtonClickScale}" shell:WindowChrome.IsHitTestVisibleInChrome="True" Command="{Binding CloseApplicationCommand}" Padding="0" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="{DynamicResource PrimaryHueLightForegroundBrush}" Width="20" Height="20" Margin="0 0 20 0">
                <Image RenderOptions.BitmapScalingMode="HighQuality" Source="/Assets/Images/close-icon-white.png" Width="20"></Image>
            </Button>
        </WrapPanel>
    </Grid>
</UserControl>
